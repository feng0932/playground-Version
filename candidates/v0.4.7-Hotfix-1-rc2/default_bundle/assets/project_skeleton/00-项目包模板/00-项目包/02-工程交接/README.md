# 项目级工程交接正式目录索引

本目录用于承接项目级 Gate B 技术真源，不是 `PRD` 正文，也不是 `80-完整提交包/` 展示层。

## 当前有效真源

- 项目级工程交接入口：当前 README
- 跨模块共享规则：按专题子目录收纳 `FE / BE / QA / DBA / Ops/SRE` 正式文件
- trigger-required 的状态图、策略说明、生命周期说明、RBAC 细则

## 历史保留

- 仅在存在明确替换关系时保留历史文件
- 历史文件必须标注 `retired-truth marker`

## 目录规则

- 模块级工程交接资料应优先落在 `01-模块执行包/<模块名>/02-工程交接/`
- 若本轮为跨模块或项目级共用交接材料，才落在当前目录
- 不再使用“未完成工程交接”“仅提供目录入口”这类 starter 口径误导读者
- 允许放置：
  - `FE / BE / QA / DBA / Ops/SRE` 的 truth source
  - trigger-required 的状态图、策略说明、生命周期说明、RBAC 细则
- 不允许放置：
  - 用于替代 `4.2A / 4.3 / 4.4 / 7` 的产品真源
  - 仅面向汇报的阅读层摘要

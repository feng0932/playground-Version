# v0.4.8-Hotfix-1 中文发版执行说明书

## 发布后状态勘误

`v0.4.8-Hotfix-1` 已标记为 `postrelease_failed` / `quarantined`。

该版本完成过一次正式内网发布动作，但发布后 mac / Windows 真实现场证明安装执行链未闭环：第一次新安装失败，更新成功不能抵消新安装失败，安装成功 / 失败主输出仍包含过多机器内部信息，且 `00 -> 01` 项目包正式宿主写入授权与落盘 fail-closed 未闭环。

当前默认 stable 已回滚到 `v0.4.7-Hotfix-1`。`v0.4.8-Hotfix-1` 只保留为失败发布审计资产，不得继续作为默认安装版本。

## 1. 这个版本做了什么

这是一次失败发布的保留说明，不再作为安装或更新目标。

本版本原计划修复安装执行链验收缺口和安装交接体验问题，但正式发布后的真实现场证明：新安装链路、主输出友好性、`00 -> 01` 写入授权、项目包落盘 fail-closed 与跨平台现场验收仍未闭合。

因此本文件的当前作用只有三个：

1. 明确该版本已 quarantine，不能继续指引用它做新安装或更新。
2. 保留失败发布审计背景，防止后续把它误认为可用 stable。
3. 指向安全默认基线：使用默认 stable 回退版本 `v0.4.7-Hotfix-1`，或等待下一热修版本。

## 2. 让大模型去安装和更新的提示词

`v0.4.8-Hotfix-1` 不提供安装或更新提示词。

需要继续使用 ai-team 时，要求大模型使用默认 stable 入口，不要显式指定 `v0.4.8-Hotfix-1`。若任务目标是验证本失败版本，只能作为审计复现，不得作为正式安装、更新或发布放行依据。

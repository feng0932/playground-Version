# PRD 前置合规检查报告示例

## 检查范围

- 检查口径：[prd-precheck-checklist.md](./prd-precheck-checklist.md)
- 扫描根目录：`<PRD 文档根目录>`
- 报告保存路径：`04-过程文件/<项目名>-prd-precheck-report.md`
- 扫描文档总数：`X`
- 已读取文档数：`X`
- 未解析文档数：`X`

### 已读取文档

- `<文件1>`
- `<文件2>`
- `<文件3>`

### 未解析文档

- `<文件A>`：`docx / xmind / pdf扫描件 / 其他不可直接读取格式`
- `<文件B>`：`二进制或受限文件`

## 总体结论

`可进入设计 / 建议补充 PRD 后进入设计 / 不建议进入开发`

结论说明：

- 当前审查已覆盖输入根目录下全部可读文档。
- 以下结论仅基于已读取材料，不代表未解析文件已完成审查。

统计结果：

- `阻断项`：X
- `高风险项`：X
- `提示项`：X

## 主要问题

| 编号 | 风险等级 | PRD章节 | 合规主题 | 检查项 | 当前问题 | 影响 | 整改建议 | 证据 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R01 | 阻断项 | 用户体系 | 用户权利 | 账号注销能力是否前置考虑 | PRD 仅定义登录与退出，没有定义注销路径。 | 进入开发后将直接缺少核心用户权利链路。 | 在设置或账户中心补充注销入口、删除范围和处理反馈。 | `[文件路径:行号]` |
| R02 | 高风险项 | 数据需求 | 敏感信息 | 是否识别敏感个人信息 | 文档写了照片上传和城市定位，但未区分必要/可选，也未定义拒绝后流程。 | 容易在详细设计阶段演变为超范围收集和高敏索权问题。 | 补充字段级数据清单和拒绝授权后的替代路径。 | `[文件路径:行号]` |

## 证据摘录

### 1. 目录覆盖情况

- 已对输入根目录进行了递归扫描。
- 本次结论覆盖全部可读文本文件。
- 未解析文件已单独列出，未默认视为已审查。

### 2. 业务范围和模块覆盖

- `<引用一条说明产品定位或业务范围的证据>`
- `<引用一条说明模块范围或版本规划的证据>`

### 3. 数据、权限或账号相关证据

- `<引用一条数据字段、权限、登录或注销相关证据>`
- `<引用一条说明文档缺失关键规则的证据>`

## 已具备的正向基础

- `<已有清晰业务类型定义>`
- `<已有协议、客服、常见问题或基础账号能力>`
- `<已有结构化字段或流程描述>`

## 优先整改顺序

1. 先补 `账号权利 + 隐私/投诉/删除/注销`。
2. 再补 `高敏数据字段 + 权限触发点 + 拒绝后流程`。
3. 然后补 `商业化规则 + 平台专项约束`。
4. 最后补 `内容治理 + 未成年人或场景边界`。
